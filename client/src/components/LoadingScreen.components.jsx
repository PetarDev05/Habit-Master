const LoadingScreen = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center font-semibold z-60 text-7xl text-(--white) bg-[conic-gradient(from_0deg,var(--blue),var(--yellow),var(--pink),var(--blue))]">
      Habit Master
    </div>
  );
};

export default LoadingScreen;
